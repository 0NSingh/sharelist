import { FC } from "react";
interface  PaginationBarProps {};

export const PaginationBar: FC< PaginationBarProps> = (props) => {
    return (
        <div className="w-full text-white border-2 border-white mt-4 mr-4 mb-4 p-4">
            PAGINATION BAR
        </div>
    );
}
