import { FC } from "react";
interface  OptionsBarProps {};

export const OptionsBar: FC< OptionsBarProps> = (props) => {
    return (
        <div className="w-full h-full p-4 border-2 border-white">
            
        </div>
    );
}
