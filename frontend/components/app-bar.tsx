import { FC } from "react";
interface  AppBarProps {};

export const AppBar: FC< AppBarProps> = (props) => {
    return (
        <div className="border-2  border-white w-full h-20 rounded-b-3xl">
            
        </div>
    );
}
