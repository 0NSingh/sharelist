import { FC } from "react";
interface  SideBarProps {};

export const SideBar: FC< SideBarProps> = (props) => {
    return (
        <div className="w-1/3 p-4 h-full border-2 border-white">
            
        </div>
    );
}
